<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <!-- Ensure the document is rendered with the correct character encoding -->

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Make the page responsive to different screen sizes -->

    <title>Todo List</title>
    <!-- Title of the webpage -->
  </head>
  <body>
    <h1>Todo List</h1>
    <!-- Main heading of the webpage -->

    <p><%= kindOfDay %></p>
    <!-- Display the current date passed from the server -->

    <ul>
      <!-- Unordered list to display the list of tasks -->
      <% for (let i = 0; i < newTaskList.length; i++) { %>
      <!-- Loop through the array of tasks and dynamically generate list items -->
      <li><%= newTaskList[i] %></li>
      <!-- Display each task in the list -->
      <% } %>
    </ul>

    <form action="/" method="post">
      <!-- Form to add new tasks to the list -->
      <input type="text" placeholder="write your tasks" name="newTask" />
      <!-- Text input for entering a new task -->

      <button type="submit">Add Task</button>
      <!-- Button to submit the new task to the server -->
    </form>
  </body>
</html>
